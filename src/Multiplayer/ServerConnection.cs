using Network;

namespace PixelColor;

class ServerConnection(Connection connection, int UUID, string username) {
    public Connection connection = connection;
    public int UUID = UUID;
    public string username = username;
}